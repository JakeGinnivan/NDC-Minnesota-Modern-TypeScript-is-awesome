interface Feature {
    canUserToggle: boolean
    enabled: boolean
}

interface Features {
    [featureName: string]: Feature
}

const features: Features = {
    autoplayVideo: {
        canUserToggle: false,
        enabled: true
    },
    newArticlePage: {
        canUserToggle: true,
        enabled: false
    }
}