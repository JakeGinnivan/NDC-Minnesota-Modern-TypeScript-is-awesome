const flightOrLayover: Flight | Layover

if (isFlight(flightOrLayover)) {
    flightOrLayover.location // compilation error, it's a Flight
    flightOrLayover.from // get type safety inside the if block!

    return
}

flightOrLayover.location // typeof flightOrLayover === Layover

function isFlight(obj: any): obj is Flight {
    return 'from' in obj && 'to' in obj && 'duration' in obj
}